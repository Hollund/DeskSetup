# Saved profile data (local machine state, not code)
SetDesk Profiles/*.json
SetDesk Profiles/*.txt

# Shortcuts are generated by Create-Shortcuts.ps1, no need to track them
SetDesk Profiles/*.lnk

# Legacy output folder
output/
